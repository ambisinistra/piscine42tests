int main(void)
{
  int size_ = 1;
  int test_cases[1][50] = {"salut, comment tu vas ? 42mots quarante-deux; cinquante+et+un"};

  for (int i=0; i < size_; i++)
  {
    printf ("%s \n", test_cases[i]);
  }
  return (0);
}
